// App-level display config — the single source for the working utility brand.
//
// The demo is shown to many different utilities, so the curated data and SQL
// are deliberately brand-neutral. The customer-facing name lives here (and in
// the curated dim_geography/dim_utility config row) so it can be changed in one
// place. Working name: "Lakeshore Power & Light".
export const UTILITY_NAME = "Lakeshore Power & Light";

// Product name shown in the top bar and browser tab.
export const PRODUCT_NAME = "Customer 360";
